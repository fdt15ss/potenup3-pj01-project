# 데이터로 본 디지털 샤머니즘

> 검색·앱 리뷰·커뮤니티 데이터로 살펴본 운세 콘텐츠의 소비 맥락

## 프로젝트 한눈에 보기

| 항목 | 내용 |
| --- | --- |
| 프로젝트 형태 | 4인 팀 데이터 분석 프로젝트 |
| 수행 시기 | 2026년 2월 |
| 분석 대상 | 검색 트렌드, 플레이스토어 리뷰, 네이버 카페 게시글 제목 |
| 데이터 규모 | 앱 리뷰 6,925건, 카페 게시글 816건, 월별 검색 트렌드 최대 60개월 |
| 개인 기여 | Selenium 기반 데이터 수집, 플레이스토어 리뷰 수집·정제, 네이버·구글 검색 트렌드 통합, 탐색적 분석 및 문서화 |
| 주요 도구 | Python 3.11, pandas 3.0.1, Selenium 4.41.0, google-play-scraper 1.2.7, KoNLPy 0.6.0(Okt), mlxtend 0.24.0, matplotlib 3.10.8 |

## 기술 스택

실행 환경은 `uv`로 잠금(`uv.lock`)했으며, 아래는 실제 분석에 사용한 핵심 패키지의 잠금 버전입니다.

| 구분 | 스택 | 버전 | 선택 이유 |
| --- | --- | --- | --- |
| 언어·환경 | Python | 3.11 | KoNLPy·JPype1이 안정적으로 동작하는 버전대 |
| 패키지 관리 | uv (`pyproject.toml` + `uv.lock`) | — | 팀원 간 동일 버전 재현을 위해 잠금 파일 기반으로 환경 고정 |
| 데이터 처리 | pandas | 3.0.1 | 시계열 리샘플링·병합, 리뷰 테이블 정제의 중심 |
| 수치 연산 | numpy | 2.4.2 | 상관계수·빈도 집계 등 벡터 연산 |
| 동적 수집 | Selenium | 4.41.0 | 네이버 카페처럼 로그인·스크립트 렌더링이 필요한 페이지 대응 |
| 정적 수집 | beautifulsoup4 / requests | 4.14.3 / 2.32.5 | 정적 HTML 구간의 경량 파싱 |
| 앱 리뷰 수집 | google-play-scraper | 1.2.7 | `reviews_all`로 포스텔러 리뷰 6,925건 전량 수집 |
| 한국어 형태소 분석 | KoNLPy(Okt) + JPype1 | 0.6.0 / 1.5.2 | 신조어·구어체가 섞인 리뷰에서 명사 추출 |
| 연관규칙 분석 | mlxtend | 0.24.0 | Apriori·association_rules로 단어 동시 출현 관계 도출 |
| 시각화 | matplotlib + koreanize-matplotlib | 3.10.8 / 0.1.1 | 한글 폰트 깨짐 없이 그래프 산출 |
| 시각화(보조) | seaborn / WordCloud / networkx | 0.13.2 / 1.9.6 / 3.6.1 | 분포 비교, 키워드 요약, 연관 네트워크 표현 |
| 분석 환경 | Jupyter / ipykernel | 1.1.1 / 7.2.0 | 탐색적 분석과 결과 공유 |

> `pyproject.toml`에는 최소 버전(`>=`)으로 선언하고, 실제 재현은 `uv.lock`의 고정 버전을 기준으로 합니다.

## 문제 정의

사주·운세·타로는 모바일 앱과 검색 플랫폼을 통해 일상적인 콘텐츠로 소비되고 있습니다. 이 프로젝트는 다음 질문에서 출발했습니다.

> 사회적 불확실성이 커질수록 사람들은 운세 콘텐츠를 더 많이 찾고, 그 과정에서 정서적 위안을 얻는가?

이를 확인하기 위해 검색 행동, 앱 평가, 커뮤니티 고민 글이라는 서로 다른 이용자 행동 데이터를 함께 살펴봤습니다. 다만 관찰 데이터만으로 인과관계를 단정하지 않고, 각 가설이 현재 데이터로 어디까지 확인되는지를 구분했습니다.

## 가설과 검증 결과

| 가설 | 판정 | 근거 |
| --- | --- | --- |
| H1. 사회·경제적 불안이 커질 때 운세 관련 검색량이 증가한다. | 미검증 | 검색 트렌드는 확보했지만 같은 기간의 실업률·물가 등 월별 경제지표를 결합하지 않아 관계를 직접 검증할 수 없음 |
| H2. 앱 리뷰에 위로·불안·확신 등 정서적 사용 맥락이 반복된다. | 부분 확인 | 관련 표현은 발견됐지만, `재미`·`정확` 등 서비스 평가 표현보다 지배적이라고 보기는 어려움 |
| H3. 네이버와 구글에서 키워드 관심도 흐름이 유사하다. | 지지되지 않음 | 공통 36개월의 플랫폼 간 Pearson 상관계수가 운세 -0.001, 사주 -0.071, 타로 0.238로 낮게 나타남 |

가설이 모두 지지된 것처럼 결론 내리지 않은 것이 이 분석의 중요한 결과입니다. 특히 플랫폼마다 이용자 구성과 상대지수 산정 방식이 다르므로, 서로 다른 검색 지표의 합이나 단순 시각 비교만으로 보편적 관심을 주장하기 어렵다는 점을 확인했습니다.

## 데이터 구성

| 데이터 | 기간·규모 | 활용 목적 |
| --- | --- | --- |
| 네이버 데이터랩 통합 키워드 | 2021.02–2026.01, 60개월 | `사주·운세·타로` 묶음의 장기 추이 확인 |
| 네이버 데이터랩 개별 키워드 | 2023.01–2025.12, 36개월 | 운세·사주·타로·취업운의 월별 흐름 비교 |
| Google Trends | 2022.12–2025.12, 37개월 | 네이버 데이터와 공통 기간 비교 |
| 플레이스토어 포스텔러 리뷰 | 2017.07–2026.02, 6,925건 | 별점 분포, 리뷰 명사, 단어 연관성 분석 |
| 네이버 카페 게시글 제목 | 2020.02–2026.02, 816건 | 이용자가 직접 표현한 고민 주제 탐색 |
| 전국사업체조사 | 2023년 횡단면 자료 | 보조 탐색용 자료이며 최종 가설 판정에는 사용하지 않음 |

모든 검색 트렌드 값은 절대 검색량이 아닌 플랫폼 내부의 상대지수입니다. 따라서 플랫폼 간 수치의 크기를 직접 비교하거나 더한 값은 결론의 근거로 사용하지 않았습니다.

## 분석 과정

### 1. 데이터 수집

- Selenium으로 네이버 카페 게시글의 제목, 작성일, 좋아요 수를 수집했습니다.
- `google-play-scraper`로 포스텔러 앱의 메타데이터와 리뷰를 수집했습니다.
- 네이버 데이터랩과 Google Trends에서 월별 키워드 상대지수를 확보했습니다.

### 2. 정제 및 탐색

- 리뷰에서 분석에 불필요한 사용자 이미지, 앱 버전, 개발사 답변 등의 열을 제거했습니다.
- 결측치, 중복 여부, 별점 분포, 리뷰 길이를 확인했습니다.
- 플랫폼별 날짜를 월 단위로 맞추고 2023.01–2025.12의 공통 구간을 구성했습니다.

### 3. 텍스트 분석

- 정규표현식으로 특수문자를 정리하고 Okt로 2글자 이상의 명사를 추출했습니다.
- 별점 4–5점을 긍정 반응의 대리 지표로 사용해 그룹별 단어 빈도를 비교했습니다.
- Apriori 알고리즘으로 함께 등장하는 단어쌍을 찾고, lift 기준 상위 관계를 네트워크로 표현했습니다.

별점 기반 분류는 리뷰 문장의 실제 감성을 판별한 결과가 아니라 탐색을 위한 규칙입니다. 3점 이하를 하나의 비긍정 그룹으로 묶은 결과도 같은 이유로 해석에 주의가 필요합니다.

## 주요 결과

### 1. 검색 관심도는 유지됐지만, 불안과의 인과관계는 확인하지 못했습니다

네이버의 `사주·운세·타로` 통합 상대지수는 60개월 동안 58.88–100 범위였고, 2024년 7월에 최고치 100을 기록했습니다. 2022년 1월 79.36, 2023년 1월 85.26, 2024년 1월 90.05처럼 연초에 상승하는 구간도 관찰됐습니다.

이는 운세 콘텐츠에 지속적인 관심과 계절성이 있을 가능성을 보여주지만, 사회·경제적 불안이 원인이라는 증거는 아닙니다. H1을 검증하려면 실업률, 소비자물가지수, 소비자심리지수 등 동일 기간의 월별 지표를 결합하고 시차 상관 및 이벤트 효과를 별도로 분석해야 합니다.

### 2. 앱 만족도는 높았지만, 이를 곧바로 심리적 위안으로 해석할 수는 없습니다

- 평균 별점: **4.42점**
- 4–5점 리뷰: **5,926건(85.6%)**
- 5점 리뷰: **5,071건(73.2%)**
- 1점 리뷰: **516건(7.5%)**

리뷰 포함 여부를 기준으로 확인했을 때 `재미` 699건, `정확` 249건, `마음` 195건, `위로` 92건, `불안` 32건이 나타났습니다. 정서 관련 표현은 실제로 존재했지만, 이 빈도만으로 이용 목적의 중심이 불안 해소라고 단정하기는 어렵습니다. 현재 결과는 높은 이용 만족도와 정서적 표현의 존재를 보여주는 탐색적 근거로 해석했습니다.

### 3. 플랫폼별 검색 흐름은 예상보다 달랐습니다

2023.01–2025.12의 공통 36개월을 기준으로 네이버와 구글의 키워드별 Pearson 상관계수를 계산했습니다.

| 키워드 | 상관계수 | 해석 |
| --- | ---: | --- |
| 운세 | -0.001 | 선형 관계가 거의 없음 |
| 사주 | -0.071 | 선형 관계가 거의 없음 |
| 타로 | 0.238 | 약한 양의 관계 |
| 취업운 | 산출 불가 | Google Trends 값이 전 기간 0으로 분산이 없음 |

따라서 “플랫폼이 달라도 동일한 관심도 흐름이 나타난다”는 H3는 현재 데이터로 지지되지 않았습니다. 이는 플랫폼 특성, 표본 차이, 검색량이 적은 키워드의 0 처리, 각 서비스의 정규화 방식 차이를 추가로 검토해야 한다는 분석 과제를 남겼습니다.

### 4. 카페 제목에서는 진로와 관계 고민이 함께 관찰됐습니다

816개 제목에서 `고민` 41건, `결혼` 21건, `합격` 20건, `연애` 19건, `취업` 13건, `직장` 13건이 확인됐습니다. 이용자가 사주 커뮤니티에서 진로·시험·관계 문제를 함께 질문한다는 점은 관찰할 수 있었지만, 제목만으로 감정의 강도나 상담 동기를 판단하지는 않았습니다.

## 시행착오와 기술적 선택

### 시행착오 1. `requests`로 카페 목록을 못 가져온 문제 — 수집 도구를 대상별로 분리

네이버 카페 게시판은 로그인 세션과 스크립트 렌더링을 거쳐야 목록이 노출되기 때문에 `requests` + `BeautifulSoup` 조합으로는 게시글 제목을 가져올 수 없었습니다. 그래서 카페 수집은 **Selenium 4.41.0**으로 실제 브라우저를 제어해 목록 행(`find_elements`)을 순회하며 제목·작성일·좋아요 수를 추출했습니다.

반면 플레이스토어 리뷰는 같은 방식으로 스크롤을 반복하면 수집 시간이 크게 늘어나므로, 내부 API를 감싼 **google-play-scraper 1.2.7**의 `reviews_all()`을 사용해 6,925건을 한 번에 확보했습니다.

- **배운 점**: 크롤링 도구 선택의 기준은 익숙함이 아니라 *대상 페이지가 데이터를 어디서 만들어내는가*입니다. 전용 라이브러리가 있는 대상에 굳이 브라우저 자동화를 쓰면 비용만 커집니다.
- **개선할 점**: 현재 카페 수집기는 페이지 전환 후 `time.sleep(2)`로 고정 대기합니다. 네트워크가 느리면 요소를 놓치고, 빠르면 불필요하게 기다립니다. `WebDriverWait` + `expected_conditions`로 조건부 대기로 바꾸고, 요소 탐색 실패에 대한 예외 처리와 재시도를 넣어야 안정적인 수집기가 됩니다.

### 시행착오 2. KoNLPy가 임포트 단계에서 실패한 문제 — Okt 선택과 그 대가

리뷰·게시글 제목에는 “넘 잘맞아요”, “ㅋㅋ”, 이모지 같은 구어체 표현이 많아, 정규표현식으로 특수문자를 제거한 뒤 **KoNLPy 0.6.0의 Okt**로 2글자 이상 명사만 추출했습니다. Okt는 별도 사전 구축 없이 구어체를 비교적 무난하게 처리해 초기 탐색에 적합했습니다.

- **배운 점**: KoNLPy는 JVM 기반이라 **JPype1 1.5.2**와 Python 3.11 버전 조합이 맞지 않으면 임포트 단계에서 바로 실패합니다. 형태소 분석기 선택은 정확도만의 문제가 아니라 런타임 의존성 관리 문제이기도 했습니다.
- **개선할 점**: ‘사주’, ‘타로’, ‘포스텔러’, ‘오늘의 운세’ 같은 도메인 용어가 일관되게 하나의 명사로 묶이지 않는 경우가 있었습니다. 사용자 사전을 지원하는 분석기(Kiwi 등)로 도메인 사전을 추가하면 빈도 집계의 신뢰도를 높일 수 있습니다.

### 시행착오 3. 규칙 수가 폭증한 문제 — Apriori 임계값을 다시 잡은 과정

단순 빈도만으로는 “어떤 단어가 함께 쓰이는가”를 볼 수 없어, **mlxtend 0.24.0**의 `TransactionEncoder` → `apriori` → `association_rules` 파이프라인을 사용했습니다. 리뷰 한 건을 하나의 트랜잭션(명사 집합)으로 보고 `min_support=0.005`, `max_len=2`로 두 단어 쌍까지만 탐색한 뒤, **lift** 기준 상위 관계를 **networkx**로 시각화했습니다.

- `min_support=0.005`: 전체의 0.5% 미만으로 등장하는 조합을 잘라 규칙 수가 폭증하는 것을 막았습니다.
- `max_len=2`: 해석 가능한 단어쌍 수준으로 제한했습니다.
- `lift` 기준 정렬: confidence만 보면 ‘운세’처럼 원래 자주 나오는 단어가 무조건 상위에 오기 때문에, 단독 등장 확률 대비 동시 등장의 증가폭을 보는 lift를 기준으로 삼았습니다.

- **배운 점**: 연관규칙의 결과는 알고리즘이 아니라 **support·confidence·lift 임계값을 어떻게 잡았는지에 따라 달라집니다**. 그래서 결과를 제시할 때 파라미터를 함께 밝히지 않으면 재현도 반박도 불가능합니다. 또한 lift가 높은 단어쌍은 ‘의미적 연관’이 아니라 ‘함께 쓰이는 관용 표현’인 경우가 많아, 원문 리뷰를 직접 확인하는 과정이 반드시 필요했습니다.

### 시행착오 4. 감성 라벨이 없던 문제 — 별점을 ‘대리 지표’로 쓴 선택과 그 한계

감성 분류 모델을 학습시킬 라벨 데이터가 없었기 때문에, 별점 4–5점을 긍정 반응의 **대리 지표(proxy)** 로 삼아 그룹별 단어 빈도를 비교했습니다. 빠르게 탐색을 시작할 수 있는 실용적인 선택이었습니다.

- **배운 점**: 대리 지표는 “측정하고 싶은 것”이 아니라 “측정할 수 있는 것”입니다. 별점이 높아도 본문이 불만인 리뷰, 별점이 낮아도 앱 자체는 좋다는 리뷰가 존재합니다. 그래서 결과를 서술할 때 *별점 기반 분류는 감성 판별 결과가 아니다* 라는 단서를 반드시 함께 남겼습니다. 지표의 한계를 문서에 명시하는 것 자체가 분석 결과의 일부라는 점을 배웠습니다.

### 시행착오 5. 두 플랫폼 지수를 더하려다 폐기한 문제 — 상관 비교로 전환

네이버 데이터랩과 Google Trends는 모두 0–100의 값을 주지만, 각 플랫폼 내부에서 **최대값을 100으로 정규화한 상대지수**이며 모수와 산정 기준이 다릅니다. 초기에는 두 값을 더해 ‘전체 관심도’를 만들려 했지만, 이는 서로 다른 단위를 합치는 것과 같아 폐기했습니다. 대신 공통 36개월(2023.01–2025.12) 구간을 맞춘 뒤 **키워드별 Pearson 상관계수**로 흐름의 유사성만 비교했습니다.

- **배운 점**: 값의 범위가 같다고 해서 같은 단위가 아닙니다. 서로 다른 출처의 지표는 합산이 아니라 *정렬·표준화 후 관계 비교*로 접근해야 합니다.
- 이 과정에서 ‘취업운’ 키워드는 Google Trends 값이 전 기간 0이라 분산이 없어 상관계수 산출 자체가 불가능했습니다. **검색량이 적은 키워드의 0은 ‘관심 없음’이 아니라 ‘측정 임계 미만’일 수 있다**는 점도 함께 확인했습니다.

### 시행착오 6. 팀원마다 결과와 한글 폰트가 달라진 문제 — 환경 고정

- 팀원 간 환경 차이로 결과가 달라지는 것을 막기 위해 **uv**로 `pyproject.toml`과 `uv.lock`을 함께 관리해 패키지 버전을 고정했습니다.
- matplotlib에서 한글이 깨지는 문제는 폰트 경로를 직접 지정하는 대신 **koreanize-matplotlib**을 임포트하는 방식으로 해결해, OS가 다른 팀원 환경에서도 동일하게 동작하도록 했습니다.
- **개선할 점**: 현재 노트북은 셀 실행 순서에 의존합니다. 수집·정제·분석 단계를 모듈로 분리하고 중간 산출물을 파일로 저장하면, 재실행 없이도 분석 단계만 반복할 수 있는 구조가 됩니다.

### 시행착오 7. 가설이 지지되지 않은 문제 — 결론을 만들지 않기로 한 선택

가장 크게 남은 배움은 도구가 아니라 태도였습니다. 세 개의 가설 중 명확히 지지된 것은 없었고, H3는 오히려 **예상과 반대 방향**의 결과가 나왔습니다. 이때 상관계수가 낮은 키워드를 빼거나 기간을 조정해 보기 좋은 결론을 만들 수도 있었지만, 판정을 *미검증 / 부분 확인 / 지지되지 않음* 으로 구분해 그대로 남겼습니다.

- **배운 점**: 분석의 신뢰도는 결론의 선명함이 아니라 **주장과 근거 사이의 거리를 얼마나 정확히 표시했는가**에서 나옵니다. “확인하지 못했다”를 결과로 쓰는 것이 “확인했다”고 과장하는 것보다 다음 분석에 훨씬 쓸모 있는 자산이었습니다.

## 한계와 다음 개선

- 실업률·물가·소비자심리지수의 월별 시계열을 추가하고, 정상성 확인 후 교차상관과 시차 회귀로 H1을 검증합니다.
- 네이버와 구글 데이터를 각각 표준화한 뒤 단순 합산이 아닌 변화율·상관·동적 시간 왜곡(DTW) 등으로 비교합니다.
- 별점과 텍스트 감성을 분리하고, 한국어 감성 모델 또는 수작업 라벨 표본으로 분류 성능을 검증합니다.
- 카페 본문과 댓글을 개인정보 보호 기준에 맞게 추가해 제목 중심 분석의 한계를 보완합니다.
- 데이터 수집과 분석 노트북을 분리하고, 셀 실행 순서에 의존하지 않는 재현 가능한 파이프라인으로 정리합니다.

## 주요 산출물

- [네이버 카페 데이터 수집 노트북](./project01_데이터_수집_Selenium.ipynb)
- [네이버 데이터랩 장기 추이 분석 노트북](./project01_데이터랩.ipynb)
- [플레이스토어 리뷰 분석 노트북](./project01_playstore.ipynb)
- [네이버·구글 검색 트렌드 비교 노트북](<[프로젝트] 1조_데이터_트렌드_분석.ipynb>)
- [최종 발표 자료](<./output/1조 최종 발표슬라이드ver.C.pptx>)
- [발표 자료 설명](<./output/발표 자료 설명.md>)

## 한 줄 회고

> 좋은 분석은 가설을 멋지게 증명하는 일이 아니라, 데이터가 말할 수 있는 범위를 정확히 구분하는 일이다.
